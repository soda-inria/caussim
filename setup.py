#!/usr/bin/env python

from distutils.core import setup

setup(name='caussim',
      version='1.0',
      description='Causal simulation experiments',
      author='M D',
      packages=['caussim'],
     )
